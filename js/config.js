/* ============================================================
   SITE CONFIGURATION — the only file you should need to edit
   ============================================================ */
const SITE_CONFIG = {
  // Paste your Google Apps Script Web App URL between the quotes
  // after following the steps in README.md (Section 2).
  // It looks like: https://script.google.com/macros/s/XXXXX/exec
  APPS_SCRIPT_URL: "PASTE_YOUR_APPS_SCRIPT_URL_HERE",

  // Shown on the honeymoon fund page if the live number can't load
  // (e.g., before you've set up the Apps Script). Percent only — no dollars.
  FALLBACK_FUND_PERCENT: 8,

  // Wedding date/time (ceremony start, Central Time)
  WEDDING_DATE: "2026-12-12T16:00:00-06:00",
};
